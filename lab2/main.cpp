import std;
import LabUtils;
using namespace std;


void(vector<int>& vec, int n) {
    for (int i = 0; i < n; i++) {
        cin << vector()
    }
}




int main(){
    printMetadata(2, "Аббасов Теймур");
    print("Введите кол-во элементов последовательности: ");
    


    int n = 0;
    cin >> n;
    vector<int>(n, 0);
    printSeparLine(22);
    println("Выберите способ заполнения: \n[1] - Вручную. \n[2] - Случайным образом.");
    print("> ");
    int fillingMethod = 1;
    cin >> fillingMethod;
    printSeparLine(22);
    if (fillingMethod == 1) {
        for (int i)
    }



    pause();
    return 0;
}